/**
 * The main page for task management.
 * This component serves as the primary interface for users to interact with their tasks.
 * @returns A React element representing the task management page.
 */
function TaskManagement() {
  return (
    <div className="flex h-screen w-full items-center justify-center">
      <div className="text-3xl font-bold">Home Page</div>
    </div>
  );
}

export default TaskManagement;
